# SDS wrapper for Home Assistant long-lived token (C4).
# token.txt is owner-provided — not in git.
resources:
  - '@type': type.googleapis.com/envoy.extensions.transport_sockets.tls.v3.Secret
    name: ha_token
    generic_secret:
      secret:
        filename: /etc/broker/ha/token.txt
