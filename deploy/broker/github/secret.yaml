# SDS wrapper for GitHub fine-grained PAT (C5).
# token.txt is owner-provided — not in git.
resources:
  - '@type': type.googleapis.com/envoy.extensions.transport_sockets.tls.v3.Secret
    name: github_token
    generic_secret:
      secret:
        filename: /etc/broker/github/token.txt
