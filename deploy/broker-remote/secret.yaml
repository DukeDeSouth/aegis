# SDS-обёртка для remote broker (идентично deploy/broker/secret.yaml).
resources:
  - '@type': type.googleapis.com/envoy.extensions.transport_sockets.tls.v3.Secret
    name: broker_token
    generic_secret:
      secret:
        filename: /etc/broker/token.txt
